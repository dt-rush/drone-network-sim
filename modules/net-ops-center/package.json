{
  "name": "drone-network-net-ops-center",
  "version": "1.0.0",
  "description": "receives drone update protobuf messages over websocket and stores them in the db",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "drop": "node drop_db.js",
    "start": "node server.js",
    "drop-start": "npm run drop && npm run start",
    "dev": "nodemon server.js",
    "drop-dev": "npm run drop && nodemon server.js"
  },
  "author": "dt-rush",
  "license": "MIT",
  "dependencies": {
    "wait-port": "^0.2.2",
    "ws": "^6.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.3"
  }
}
