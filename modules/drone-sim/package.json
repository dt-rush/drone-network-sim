{
  "name": "drone-network-drone-sim",
  "version": "1.0.0",
  "description": "simulates a drone out in the world, sending its position, destination, and velocity over websocket to the net-ops-center via protobuf messages",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node client.js",
    "dev": "nodemon client.js"
  },
  "author": "dt-rush",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.22.2",
    "mongodb": "^3.1.1",
    "uuid": "^3.3.2",
    "wait-port": "^0.2.2",
    "ws": "^6.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.3"
  }
}
